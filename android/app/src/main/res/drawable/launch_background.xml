<?xml version="1.0" encoding="utf-8"?>
<!-- Native splash background, kept in sync with AppColors.bgLight and the
     logo used by lib/views/SplashScreen.dart so there is no visible flash
     or jump when the Flutter engine takes over. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />
    <item>
        <bitmap
            android:gravity="center"
            android:src="@drawable/launch_logo" />
    </item>
</layer-list>
