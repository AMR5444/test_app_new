<?xml version="1.0" encoding="utf-8"?>
<!-- Plain background only, matching AppColors.bgLight / the splash.webp
     background — no logo here. The logo is shown once by
     lib/views/SplashScreen.dart so the user only sees it once, not as a
     separate flash before Flutter's first frame. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />
</layer-list>